# Resets the cooldown values in the data upon reloads, so have them all in one file

# Measured in ticks
scoreboard players set #GameLimit RoomToken 300
scoreboard players set #TutorialLimit RoomToken 300
# ...
