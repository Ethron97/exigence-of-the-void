# Set setting specific description text

## CONSTRAINTS
#   AS item display (setting)

#====================================================================================================

data modify entity @s text set value [{text:"When enabled, a warning will appear when attempting to enter the dungeon with \
currency in your inventory, which may be lost.",color:"gray"}]
